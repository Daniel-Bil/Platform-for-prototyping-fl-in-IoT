# Your laptop's origin is HTTPS, but deployment hosts clone over SSH so the
# forwarded local ssh-agent can authenticate to GitHub.
repo_url: "git@github.com:Daniel-Bil/Platform-for-prototyping-fl-in-IoT.git"

# First clone uses the shared development branch, then provisioning explicitly
# switches the machine to our deployment branch.
repo_base_branch: "dev"
repo_branch: "deployment"

# Repository/environment paths on EACH remote host.
repo_dest: "{{ ansible_env.HOME }}/Platform-for-prototyping-fl-in-IoT"
deployment_dir: "{{ repo_dest }}/tools-deployment"
venv_dir: "{{ repo_dest }}/.venv-deployment"

# TensorFlow 2.14.1 has wheels for CPython 3.9-3.11, not CPython 3.12.
# Keep Ansible on the host's system Python; only the FL venv uses 3.10.
deployment_python_version: "3.10"
deployment_python_bin: "/usr/bin/python3.10"

install_python_deps: true

# Federated deployment defaults.
# Supported: FedAvg, FedProx, FedPAQ, FedMA, HierFedAvg
fl_algorithm: "FedAvg"
fl_port: 8090
fl_edge_port: 8091
fl_edge_count: 2
fl_fedprox_mu: 0.01
fl_fedpaq_bits: 8
fl_rounds: 3
fl_local_epochs: 1
fl_batch_size: 32
fl_join_window: 2
fl_initial_join_window: ""
fl_round_timeout: 300
fl_evaluation_timeout: 300
fl_evaluate_every: 1
fl_eval_batch_size: 256
# Global classification threshold is selected independently in every evaluated
# round from distributed validation confusion counts. Test labels are never
# used to tune this threshold.
fl_threshold_min: 0.0
fl_threshold_max: 1.0
fl_threshold_step: 0.01
fl_threshold_preferred: 0.5
fl_seed: 42
fl_model_path: "{{ deployment_dir }}/config/default_model.json"

# Final thesis benchmark dataset: cleaned REAL RuralIoT measurements with
# controlled split-local fault episodes (generated by tools2/05_prepare_fl_dataset.py).
fl_dataset_root: "{{ repo_dest }}/tools2/data/fl_dataset_real"

# Empty = use main's default IPv4 address discovered by Ansible.
# Override when necessary with: -e fl_server_host=192.168.x.x
fl_server_host: ""

# Optional benchmark metadata. The Python server stores these in result artifacts;
# they do NOT control how many clients the dynamic server accepts.
fl_run_id: ""
fl_campaign_id: ""
fl_repetition: 0
fl_requested_clients: 0
